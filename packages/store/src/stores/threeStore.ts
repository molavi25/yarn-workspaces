import { defineStore } from 'pinia';

export const useThreeStore = defineStore('three', {
  state: () => ({
    // Define Three.js state
  }),
  getters: {
    // Define Three.js getters
  },
  actions: {
    // Define Three.js actions
  },
});
