import { defineStore } from 'pinia';

export const useUiStore = defineStore('ui', {
  state: () => ({
    // Define UI state
  }),
  getters: {
    // Define UI getters
  },
  actions: {
    // Define UI actions
  },
});
